# Request Info

I use this tool to inspect all the headers contained in an HTTP request.

## Usage

- `npm run dev`: will show errors inside the browser, will not cache EJS files, ecc
- `npm run production`: will set NODE_ENV to "production".

## Configuration

`PROJECT_NAME` and `PROJECT_PORT` can be customized inside `.env`.